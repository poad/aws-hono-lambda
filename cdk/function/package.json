{
  "name": "@aws-hono-lambda/function",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "eslint *.ts ",
    "style-check": "prettier --write ",
    "format": "prettier --write ",
    "fix": "prettier --write  && eslint --fix *.ts "
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
